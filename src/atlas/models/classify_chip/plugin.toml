name = "classify_chip"
description = "Triage an RGB chip: bare, built-up, water, vegetation, cloud."
runtime = "centroid"
weight = "classify_chip/model.json"
sha256 = ""

[input]
kind = "rgb_png"
shape = [224, 224, 3]
dtype = "uint8"

[output]
kind = "scores"
classes = ["bare", "built-up", "water", "vegetation", "cloud"]
